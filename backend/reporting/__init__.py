"""Phase 5 report assembly -- the `[Report Export]` node of architecture.md §2.

Sits above both `engine` (the CO2 figure) and `verification` (the verdict), so
it composes them rather than either importing the other.
"""
